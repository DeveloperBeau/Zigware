# Project ideas

Expansion ideas for the framework, one file per idea. Each is a sketch for
discussion, not a commitment.

- [001 - Native frontend over an FFI/IPC bridge](001-native-frontend-bridge.md):
  drive the Zig command and security core from a native UI such as SwiftUI,
  instead of the HTML/JS webview.
